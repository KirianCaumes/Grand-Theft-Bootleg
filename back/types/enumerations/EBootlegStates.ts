export enum EBootlegStates {
    DRAFT,
    PENDING,
    PUBLISHED,
    DELETED
}

//Used to check creation from user
export enum EBootlegStatesUser {
    DRAFT,
    PENDING
}

//Used to check creation from moderator
export enum EBootlegStatesModerator {
    DRAFT,
    PENDING,
    PUBLISHED
}